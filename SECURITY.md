# 安全与隐私

## 使用边界

映言设计为 Windows 单用户本地应用，只监听 `127.0.0.1:8765`。不应作为公网服务或多租户平台使用，也不应通过隧道/公网反向代理开放给其他人。

应用可以读取用户在网页内选中的本地目录。保持其绑定、本地 Host/Origin 检查和会话机制，不要为方便访问移除它们。它不是操作系统级隔离沙箱，不防御已控制本机账户的恶意程序。

## 数据

- 视频直接在原位置读取，不作为上传内容送到云端。
- 内部任务数据、路径、识别结果和 ZIP 默认保存在 `data/`。
- 只有用户确认分发才会在源目录新增结果；不覆盖已有同名文件。
- 源码仓库排除 `data/`、模型缓存、依赖、日志、媒体与个人验收记录。
- 依赖安装和主动下载模型需要访问上游服务器；不要把“本地推理”等同于安装全过程完全离线。

## 报告问题

怀疑有任意路径写入、跨站请求、敏感数据泄漏或覆盖已有文件等问题时，请先停止受影响的服务。

优先使用仓库的私密漏洞报告入口（如果维护者已启用），或通过 GitHub 个人主页提供的渠道联系维护者。若暂时只能提交公开 Issue，请只描述问题类别和版本，不贴可利用细节、令牌、真实文件路径、视频或文字稿；等待维护者提供私密渠道。

不要在 Issue 中上传 `.env`、数据库、完整日志或密钥。依赖、驱动和模型的安全更新也需要单独关注。
