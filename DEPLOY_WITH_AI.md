# 把这段话交给你的 AI 智能体

适用于**能够操作你本人 Windows 电脑的终端和文件**的智能体。如果 AI 只有聊天能力、只能访问网页，或只在远程沙箱中运行，它不能据此完成你电脑上的安装。

这是 AI 辅助本地部署，不是云端在线工具，也不是包含全部模型的便携离线包。安装依赖及主动下载模型需要网络和磁盘空间。先确认你信任所用智能体，再授予所需的本机权限。

## 直接复制下面整段

```text
请帮我在当前 Windows 电脑部署开源项目“映言”。
仓库：https://github.com/fu6868/video
部署规范：仓库 docs/AI_DEPLOYMENT.md

请实际执行部署与验证，不要只把教程复述给我。
先确认你的终端和文件工具确实连接到我的本机，而不是云端沙箱；无法确认就询问，不要在别的机器部署后声称我的电脑已安装。

先只读检查系统、Python 3.11、Node/npm、项目目录、剩余空间、已有模型及端口8765，阅读 README 和部署规范，再给我一份简短计划。已有项目要保留；没有项目时先确认安装位置。

我允许你在确认的项目目录内创建虚拟环境、安装项目依赖、构建网页，并准备项目支持的本地模型。下载模型前报告来源、现有缓存、预计下载量和磁盘要求；无法可靠估算就明确说明并等我确认。当前环境检查要求 small 和 medium 两个模型，请以实际代码为准，不要只装一个却报告完整部署成功。

不删除已有环境、模型、数据库、结果或视频，不扫描与本任务无关的个人目录；不读取或索要密码和访问令牌，不关闭TLS校验、安全软件或防火墙，不开放公网访问。
需要安装系统级软件、提权、修改全局设置、重启电脑、停止已有进程或修复源码时，先说明影响并征得我同意。下载失败不得自行切换未知镜像。

逐步执行，检查每一步的退出码；遇到错误先定位，不反复重装。网页只监听127.0.0.1。完成依赖检查、模型文件检查、构建、健康检查以及允许执行的推理测试。未执行的测试明确标“未验证”，不能把模型文件存在或网页能打开当作真实转写成功。

真实测试只用我明确指定的短素材，未提供时可以先用合成音频验证解码，但不能冒充语音识别测试。不要替我扫描课程库或向真实视频目录分发结果。

最后告诉我：安装位置、实际运行设备、已验证和未验证项、网页地址、下次启动/停止方式。把不含密钥的部署报告保存到项目 data/logs/ai-deployment-report.md，不自动提交或推送仓库。
```

## 你可能需要回答的几个问题

- **装在哪里？** 选一个你有写权限、空间足够的本机目录。
- **是否允许下载模型？** 模型不在源码 ZIP 里，不要把源码体积当成完整安装体积。
- **缺 Python/Node 怎么办？** 智能体应先告诉你缺什么，再由你同意安装；不需要把整台电脑的无限权限一次交出去。
- **是否有可测试的短视频？** 最好用自己录制、没有敏感信息的几十秒素材。

完整操作顺序、命令示例、故障处理和验收清单见 [AI 部署规范](docs/AI_DEPLOYMENT.md)。
